# 작업 메모 (임시)

제출 전 `README.md`로 정리해 옮기고 이 파일은 지운다.
→ 옮길 항목 목록은 `docs/ASSIGNMENT.md`의 "README에 담을 내용" 참고.

---

## 기술 선택과 이유

### 패키지

| 패키지 | 왜 |
| --- | --- |
| `flutter_svg` | Figma 시안 아이콘을 SVG 그대로 사용하기 위해. Material 기본 아이콘(`Icons.*`)은 획 두께와 모양이 시안과 미묘하게 달라 시안 재현도가 떨어졌다. |

### 아키텍처 / 상태관리

`provider` + `ChangeNotifier`를 쓴다. 화면 세 개가 관심 상태를 공유해야 하는데,
상세 화면은 `Navigator.push`로 뜨기 때문에 `RootScreen`의 자식이 아니다.
`RootScreen`에 `setState`로 들고 있으면 상세 화면까지 내려보낼 방법이 없어서,
**`MaterialApp`보다 위**에 Provider를 두고 앱 어디서나 꺼내 쓰는 구조로 갔다.

| 저장소 | 들고 있는 것 | 왜 전역인가 |
| --- | --- | --- |
| `FavoritesStore` | id → `Stock` (이름 · 종목코드 · 시장) | 관심 상태가 세 화면에서 어긋나면 안 된다 (필수 항목) |
| `QuoteStore` | symbol → `Quote` | 관심 · 상세가 같은 시세를 쓴다 |
| `DailyPriceRepository` | symbol → 일별 시세 구간 | 화면을 나갔다 들어와도 받은 구간을 다시 받지 않기 위해 |

화면 하나에서만 쓰는 상태(정렬 기준, 검색어, 선택된 기간 탭, 토스트)는
전역으로 올리지 않고 각 화면의 `State`에 둔다.

---

## 데이터 연결 — 요청 횟수를 줄이기 위한 결정

과제 채점 기준의 "페이지 요청을 필요한 만큼만 하고 재사용하도록 구성했는지"에
해당하는 부분이다. 네 가지를 정했다.

### 1. 관심 목록은 종목명 · 시장까지 함께 저장한다

`FavoritesStore`가 id만 들고 있으면, 관심 화면을 그릴 때 종목명을 모르므로
종목 10개면 메타데이터 endpoint를 **10번** 호출해야 한다.

그런데 관심 등록은 **검색 화면에서만** 일어나고, 별을 누르는 그 순간 화면에는
이미 종목명과 시장이 떠 있다 (검색 자동완성 응답에 `name` / `typeName`이 들어있다).
그래서 등록 시점에 `Stock`을 통째로 저장한다.

- 관심 화면 진입 시 메타 요청 **0회**
- 이름 · 시장은 **즉시** 그려지고, 가격만 비동기로 채워진다
  → 시안의 스켈레톤(`feedbackSkeleton`)이 자연스럽게 이 상태를 나타낸다

### 2. 시세는 한 번의 요청으로 일괄 조회한다

`query=SERVICE_ITEM:005930,000660,035720` 처럼 종목코드를 쉼표로 이어 붙인다.
종목마다 호출하면 관심 10개에 왕복 10번이고, 새로고침을 누를 때마다 반복된다.
공개 endpoint라 호출이 잦으면 응답이 느려지거나 차단된다.

### 3. 일별 시세는 부족한 구간만 받는다

기간 탭을 옮길 때 이미 받은 구간은 재사용하고 모자란 구간만 추가로 요청한다.

| 조작 | 신규 요청 | 재사용 |
| --- | --- | --- |
| 1개월 | 약 20일 | — |
| → 3개월 | 약 40일 | 20일 |
| → 6개월 | 약 60일 | 60일 |
| → 1개월 | **0** | 전부 |
| → 1년 | 약 125일 | 120일 |

### 4. 캐시는 화면이 아니라 전역에 둔다

일별 시세 캐시를 `_DetailScreenState`에 두면 뒤로가기로 화면이 사라질 때
같이 사라져서, 같은 종목을 다시 열 때마다 처음부터 다시 받는다.
`DailyPriceRepository`를 `main.dart` 최상단 Provider에 두어 앱이 살아있는 동안
유지되게 했다. 종목별로 나뉘어야 하므로 `Map<symbol, List<DailyPrice>>` 형태다.

### DTO와 모델을 나눈 이유

Naver 응답 필드명이 `nv`(현재가) · `pcv`(전일 종가) · `cd`(종목코드) 처럼
축약되어 있다. 이 이름을 화면까지 끌고 가면 코드를 읽을 수 없어서,
`data/dto/`는 응답 모양 그대로 받고 `model/`에서 앱이 쓰는 이름으로 바꾼다.

### EUC-KR 응답 처리

실시간 시세(endpoint 2)는 `Content-Type`이 UTF-8이 아니라 **EUC-KR**이다.
바이트를 그대로 UTF-8로 디코딩하면 종목명(`nm`)이 깨진다.
(문서에는 일별 시세 HTML만 인코딩 주의로 적혀 있으나, 실제로는 이 endpoint도 같다)

다만 이 응답에서 우리가 쓰는 필드는 `cd` / `nv` / `pcv` / `ov` / `hv` / `lv` /
`aq` / `countOfListedStock` 로 **전부 숫자이고, 종목명은 사용하지 않는다**
(이름은 `FavoritesStore`에 이미 있다). → (구현 시 방식 확정 후 여기에 기록)

---

### 차트

- (미정)

---

## 디자인 토큰 관련

### 추가한 토큰

`lib/theme/app_dimens.dart`에 두 개를 추가했다. **기존 토큰 값은 수정하지 않았다.**
(대응표는 `lib/theme/README.md` 하단 "추가한 토큰" 참고)

| 필드 | 값 | 왜 |
| --- | --- | --- |
| `iconLg` | 22 | 탭 바 아이콘 · 검색 결과 행의 관심 별 · 상세 헤더의 관심 별이 모두 22. `iconSm`(16) / `iconMd`(20) 어디에도 없어 화면마다 값을 직접 쓰게 되므로 토큰화. |
| `fieldHeight` | 40 | 검색 입력창 높이가 시안에서 40 고정. `rowMinHeight` / `tabBarHeight`와 같은 성격의 컴포넌트 고정 높이 토큰. |

### 토큰을 쓰지 않고 값을 직접 쓴 곳

| 위치 | 값 | 왜 |
| --- | --- | --- |
| 각 화면의 `fontSize` | 시안 값 | 과제 안내대로 글자 크기는 토큰에 정의되어 있지 않아 Figma 텍스트 레이어 값을 그대로 사용. |

---

## Figma와 다르게 구현한 부분

- (없음)

---

## Figma에 정의되지 않아 직접 판단한 부분

> 과제에서 "이 부분을 비중 있게 본다"고 명시한 항목들.

- [ ] 로딩 표시 방식
- [ ] 네트워크 에러 처리
- [ ] 긴 종목명 텍스트 오버플로
- [x] **토스트 노출 시간과 사라지는 방식** — 아래 참고
- [ ] 시세를 아직 못 받은 행의 정렬 위치 (`현재가순` / `등락률순`)
- [ ] 검색어가 매우 길 때 빈 상태 문구 처리

### 토스트 (`lib/search_screen.dart`)

Figma에 노출 시간과 퇴장 방식이 정의되어 있지 않아 다음과 같이 정했다.

| 항목 | 결정 | 근거 |
| --- | --- | --- |
| 노출 시간 | **2초** | 한 줄짜리 짧은 문구라 읽는 데 충분하고, 검색 결과를 계속 탐색하는 화면이라 오래 떠 있으면 방해가 된다. |
| 퇴장 방식 | 즉시 제거 (애니메이션 없음) | 등장/퇴장 애니메이션은 과제의 **선택** 항목이라 생략했다. |
| 연속 탭 | 이전 `Timer`를 `cancel()` 하고 재시작 | `Future.delayed`로 하면 먼저 시작한 타이머가 나중 토스트를 조기에 지운다. 마지막으로 누른 시점부터 항상 2초가 보장되도록 `Timer`를 필드로 들고 취소한다. |
| 화면 이탈 | `dispose()`에서 타이머 취소 + 콜백에서 `mounted` 확인 | 토스트가 떠 있는 동안 탭을 옮기면 사라진 위젯에 `setState`가 호출되어 예외가 난다. |

위치는 `Stack` + `Positioned`로 화면 하단에 겹쳐 띄운다. 좌우/하단 여백은 `space4`(16).

---

## 위젯 분리 기준

### 묶은 것 — `EmptyState` (`lib/widgets/empty_state.dart`)

관심 없음 · 검색 전 · 검색 결과 없음 세 화면의 빈 상태가 시안에서 레이아웃과
간격, 색이 모두 같았다 (아이콘 40 → 12 → 제목 19/Bold/`textSecondary` →
12 → 안내 11/Regular/`textTertiary`/가운데). 바뀌는 것은 아이콘 이름과 두
문구뿐이라 파라미터 3개(`icon` / `title` / `message`)를 받는 위젯 하나로 묶었다.

복사해 두면 시안이 바뀔 때 세 군데를 모두 고쳐야 하고, 한 곳을 빠뜨리면 서로
어긋난다.

### 묶지 않은 것 — `StockRow` / `SearchResultRow`

두 행은 **왼쪽(종목명 + `코드 · 시장`)이 완전히 같지만** 일부러 분리해 두었다.
오른쪽이 각각 "현재가 + 등락"과 "관심 별"로 성격이 다르고, 하나로 합치면
`price` / `change` / `isFavorite` / `onTapStar` 처럼 서로 쓰이지 않는 파라미터가
한 생성자에 섞여 호출부에서 무엇을 넘겨야 하는지 알기 어려워진다.

공통된 왼쪽만 따로 위젯으로 빼는 방법도 있지만, 지금은 두 화면뿐이라
중복 20줄을 없애려고 계층을 한 겹 더 만드는 쪽이 오히려 읽기 어렵다고 판단했다.
세 번째 화면에서 같은 형태가 또 나오면 그때 분리한다.

### 화면 분기

- 관심 화면: `stocks.isEmpty` 두 갈래 (`stocks`는 `FavoritesStore`로 거른 결과라
  별을 모두 끄면 자동으로 빈 상태가 된다)
- 검색 화면: `_query.isEmpty` → `_results.isEmpty` → 결과, 세 갈래 early return.
  검색어가 비면 결과도 비므로 **순서가 중요하다.** 반대로 두면 검색 전에도
  "결과 없음"이 뜬다.

---

## 검색어 하이라이트 (`lib/widgets/search_result_row.dart`)

`Text`는 글자 전체에 스타일이 하나만 적용되므로, 일치 구간만 색을 바꾸기 위해
`RichText` + `TextSpan` 조각 목록으로 구현했다.
`_highlightSpans()`가 종목명을 앞 / 일치 / 뒤 세 조각으로 잘라 가운데에만
`searchHighlight`를 준다.

의도적으로 단순화한 지점 (면접 질문 예상 지점):

| 지점 | 현재 동작 | 판단 근거 |
| --- | --- | --- |
| 첫 일치만 강조 | `indexOf`로 첫 위치만 찾는다 | 국내 종목명에서 같은 검색어가 두 번 반복되는 경우가 사실상 없다. 필요하면 `indexOf`를 반복 호출하는 루프로 확장 가능. |
| 초성 검색 미지원 | `ㅅㅅ` → `삼성전자` 안 됨 | 요구사항에 없고, 검색 자체를 자동완성 API(서버)가 수행하므로 클라이언트가 매칭 규칙을 흉내 낼 이유가 없다. |
| 서버 매칭 ≠ 하이라이트 기준 | 서버가 내려준 결과라도 종목명에 검색어가 없으면 강조 없이 원문 표시 | 네이버 자동완성의 매칭 규칙을 알 수 없다. 화면에 보이는 문자열과 입력값의 단순 일치로만 처리하고, 일치가 없으면 원문을 그대로 보여주는 쪽이 예측 가능하다고 판단. |
| 대소문자 무시 | 양쪽 `toLowerCase()` | `sk`로 검색해도 `SK하이닉스`가 강조되도록. 한글에는 영향 없음. |

종목코드로 검색하면 하이라이트가 붙지 않는다. 시안이 강조하는 대상은 **종목명**이고
`005930`은 종목명 안에 없으므로 강조할 구간이 없다. 의도한 동작이다.

---

## 구현 범위

### 필수 - 완료
- [x] 하단 탭 바로 관심 / 검색 전환 (`navActive` / `navInactive`)

### 필수 - 남은 것
- (관심 화면 목록 · 빈 상태 · 정렬 / 검색 화면 전체 / 상세 화면 전체 / 상태 동기화)

### 추가로 구현한 선택 항목
- 없음

---

## 막혔던 지점

- `GestureDetector`가 자식이 그려진 픽셀 위에서만 터치를 받아 탭 바의 여백 영역이 눌리지 않았다. `behavior: HitTestBehavior.opaque`로 해결.
- `TextField`의 `prefixIcon`이 내용과 무관하게 최소 48×48 박스를 잡아, 16짜리 아이콘을 넣어도 시안과 여백이 맞지 않았다. `InputDecoration`으로 시안을 맞추는 대신 `Container` + `Row`로 입력창을 직접 조립하고, `TextField`는 밑줄·기본 여백을 끈 채(`InputBorder.none`, `isDense`, `contentPadding: zero`) `Expanded` 안에 넣는 방식으로 바꿨다.

---

## 실행 환경

- Flutter: (버전 확인해서 채우기 — `flutter --version`)
- 확인한 플랫폼: iOS 시뮬레이터 / macOS
- 폰트: 제공된 `NotoSansKR` 그대로 사용 (변경 없음)
